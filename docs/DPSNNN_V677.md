# 단편선 v6.77 검증

## 실제 사이트 조회 실측 (2026-09-05)

강남 `/booking/html_list.cm`, 미오픈 2026-09-20, 실제 엔진의 헤더·쿠키·요청 필드를 사용했다.
예약 생성·결제 요청 없음. 비교에서는 기존 프로그램 내부 4개 제한을 제외했다.
최종 비교는 초당 요청 시작 상한 32, 각 병렬 수의 두 배만큼 유한 조회했다.

| 병렬 | 요청 수 | 처리량/초 | 중앙 RTT ms | p95 RTT ms |
|---:|---:|---:|---:|---:|
|16|32|9.909|1166.745|1828.99|
|24|48|14.639|1177.6|1623.79|
|32|64|17.315|1126.165|1463.66|

모든 최종 비교 응답 HTTP 200. 측정 범위에서 32가 최고 처리량이므로 슬라이더/신규 기본값은 32.
**32까지 뚜렷한 병목을 발견하지 못했다. 절대 최적값 또는 33부터 느려진다는 증거가 아니다.**
짧은 배치의 초기 연결 비용이 포함되며 야간 오픈 혼잡, 다른 지점, 실제 주문 속도를 대신하지 않는다.
기존 저장된 슬라이더 값은 유지한다. 여러 프로그램의 합산 HTTP 동시는 같은 사용자 데이터 폴더/호스트 기준 최대 32개,
일반 요청 시작은 초당 최대 32개이며 실제 유효 처리량은 RTT/대기/사이트 상태에 따라 낮아진다.
상세 준비·주문은 대기 중 일반 조회보다 우선하고, 429/5xx 지연도 공유한다. 세션·쿠키·예약 성공/중지는 예약별 독립이다.
새 달력의 브라우저 요청은 HTTP governor의 범위 밖이며, 달력 자체의 기존 조회 간격을 따른다.

## 창 표시 및 다중 실행

실제 배포 코드(프로세스 공유 제어 포함)의 최종 추가 측정:

| 병렬 | 처리량/초 | 중앙 RTT ms (대기 포함) | p95 ms |
|---:|---:|---:|---:|
|16|9.027|1227.87|1925.56|
|24|12.234|1324.02|1660.23|
|32|13.128|1533.815|2371.77|

합산 제어 포함 시에도 32가 최고 처리량이지만 24 대비 증가폭은 약 7.3%이고 꼬리 지연이 늘었다.
따라서 32를 처리량 기준 측정 상한으로 선택했으며 개별 응답 지연 최소값이나 무제한 실행 보장이 아니다.
공유 제어 초기 구현의 디스크 동기화 병목(6.534회/초)을 발견해 일시적 스케줄 상태만 WAL/NORMAL로 조정했다.
중복 주문 방지용 OrderJournal의 디스크 동기화는 변경하지 않았다.
Windows 전체 테스트 1,132개 및 UI 검증 9개 통과.

- Chrome을 headless로 미리 준비하며 기존 고정 Chrome 슬롯 4개를 사용하지 않는다.
- 예약번호를 동반한 성공 이후에만 별도 보이는 브라우저로 접수 완료 URL을 연다. 쿠키/로컬 저장소는 해당 예약에서만 전달한다.
- 이 단계에서는 최종 주문/결제를 다시 제출하지 않는다. 화면 표시 실패가 예약 성공을 실패로 바꾸지 않는다.
- 불명확한 결과는 재주문하지 않고 로컬 주문 기록/알림톡/예약 조회로 확인한다.
- 실제 독립 프로세스 4개의 headless 준비: 0.794/0.817/0.816/0.817초, 모두 정상 종료.
- 서로 다른 프로세스의 합산 요청 상한, 모두 진행, 중지 대기 정리, Retry-After 공유, 종료된 프로세스의 점유 회수 회귀 검사 포함.

## 미오픈 날짜 확인만 수행

기존 로그 2026-09-05 22:21:52~22:22:04에서 9월 20일의 오픈 예정 9월 14일 00:00,
시간표 HTTP 200, 슬롯 게시 대기, 백그라운드 준비 전 단계 연결을 확인했다.
시간표 RTT는 약 835~1019ms였다. `40ms 후 재조회`는 서버 응답시간이 아니라 응답 후 대기시간이다.
기존 미오픈 달력/목표 날짜/테마/시간 검증 로직은 변경하지 않았다.
게시 후 정확한 슬롯 선별·중복 주문 차단·결제 접수 확인은 모의 검증 통과.
**실제 오픈 순간의 선점 시간, 여러 예약 모두의 성공, 실제 예약번호 발급은 이번 작업에서 실측하지 않았다.**
이전 성공 경험이나 HTTP 200만으로 이를 보장하지 않는다. 무통장 접수 성공은 입금 대기이며 입금 후 확정과 구분한다.
