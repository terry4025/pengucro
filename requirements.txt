customtkinter>=5.2.0
pillow>=10.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
cryptography>=43.0.0,<46
pyinstaller>=6.0.0
aiohttp>=3.9.0
playwright>=1.45.0
pywin32>=306; platform_system == "Windows"
# Windows.Media.Ocr projection used by engines/phobiadungeon_engine.py.
# It was imported at runtime but never declared, so the Phobia Dungeon engine
# failed immediately on a clean install.
winsdk>=1.0.0b10; platform_system == "Windows"
